{
  "name": "whale-dividend-keeper",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Whale.fun dividend keeper — permissionless distributeTo / vault snowball driver (gas-only hot wallet).",
  "dependencies": {
    "ethers": "^6.13.4"
  }
}
